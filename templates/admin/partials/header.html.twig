<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <a class="navbar-brand" href="{{ path('home') }}">Symfbnb</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarColor01" aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarColor01">
        <ul class="navbar-nav mr-auto">
            <li class="nav-item active">
                <a class="nav-link" href="{{ path('home') }}"><i class="fas fa-chart-line"></i> Dashboard</a>
            </li>
            <li class="nav-item active">
                <a class="nav-link" href="{{ path('admin_ads_index') }}"><i class="fas fa-hotel"></i> Annonces</a>
            </li>
            <li class="nav-item active">
                <a class="nav-link" href="{{ path('admin_bookings_index') }}"><i class="fas fa-check-circle"></i> Réservations</a>
            </li>
            <li class="nav-item active">
                <a class="nav-link" href="{{ path('admin_comments_index') }}"><i class="fas fa-comments"></i> Commentaires</a>
            </li>
            <li class="nav-item active">
                <a class="nav-link" href="{{ path('admin_users_index') }}"><i class="fas fa-users-cog"></i> Utilisateurs</a>
            </li>
        </ul>
        <ul class="navbar-nav ml-auto">
            <li class="nav-item">
                <a href="{{ path('home') }}" class="nav-link" target="_blank"><i class="far fa-eye"></i> Voir le site</a>
            </li>
            {% if app.user %}
                <li class="nav-item dropdown">
                    <a href="#" class="nav-link dropdown-toggle" data-toggle="dropdown" id="accountDropdowmLink">
                        <img src="{{ app.user.picture }}" class="avatar avatar-mini" alt="Avatar de {{ app.user.fullName }}" title="Avatar de {{ app.user.fullName }}">
                        {{ app.user.fullName }}
                    </a>
                    <div class="dropdown-menu dropdown-menu-right" aria-labelledby="accountDropdowmLink">
                        <a href="{{ path('admin_account_logout') }}" class="dropdown-item">Déconnexion</a>
                    </div>
                </li>
            {% endif %}
        </ul>
    </div>
</nav>

{% for label, message in app.flashes %}
    <div class="container">
        <div class="alert alert-{{ label }}">
            {% for message in message %}
                <p>{{ message | raw }}</p>
            {% endfor %}
        </div>
    </div>
{% endfor %}